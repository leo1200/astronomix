{
  "gpu_model": "NVIDIA H100",
  "num_gpus": 4,
  "num_processes": 1,
  "num_nodes": 1,
  "partition": "dev_accelerated-h100",
  "nodelist": "hkn0909",
  "slurm_job_id": "4170709",
  "x64": false,
  "setup": "hydro",
  "phase": "strong_scaling",
  "solver_filter": "fd_pallas",
  "block_shape": [
    4,
    4,
    8
  ],
  "num_timesteps": 8,
  "strong_N": [
    128,
    256,
    384,
    512,
    640
  ],
  "note": "Pallas-only full-ladder run; completed N<=640 (524M cells). N=768 stopped on a MEMORY ceiling, not compilation: the 1-GPU baseline (905M cells, unsharded) needs ~118 GiB peak HBM > 94 GiB H100, so XLA OOMed. The OOM did not raise cleanly; the subsequent 4-GPU run hung in an NCCL 'Acquire clique' rendezvous (collective waits hang rather than fail fast), idling to the 1h SLURM timeout. Single-GPU baseline ceiling on H100 ~= N=700 (94 GB / ~60 B/cell). Every completed rung checkpointed."
}
